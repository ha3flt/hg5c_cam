<Window x:Class="hg5c_cam.Dialogs.AppMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="360"
        MaxWidth="640"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="IconTextBlock" Grid.Column="0" FontSize="24" VerticalAlignment="Top" Margin="0,0,0,4"/>
            <TextBlock x:Name="MessageTextBlock" Grid.Column="2" TextWrapping="Wrap" MinWidth="280" MaxWidth="560"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="PrimaryButton" Width="90" Margin="0,0,8,0"/>
            <Button x:Name="SecondaryButton" Width="90" Margin="0,0,8,0" Visibility="Collapsed"/>
            <Button x:Name="TertiaryButton" Width="90" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
