***
Config file generation:
7z a -pmypassword hg5c_cam_settings.7z hg5c_cam_settings.cnf

Installation with log:
msiexec /i hg5c_cam.Telepítő.msi /l*v install.log

- Version: 1.2.build (increasing number, max. 5 digits?)

*****
- If the active camera is not a sound-enabled one, the audio playback doesn't start at first;
    it only starts after switching modes back and forth many times. Are the playback sounds mixed
    and audible, or is only the sound from the selected camera audible?
- Sometimes shortcuts won't work when I change the active camera by clicking on them!
((- Use the third stream on the Dahua cams. Try Automatic aspect ratio.))

- Add global sound on/off menu and switch. Put in on the right of the toolbar.
- Repair install won't replace and fix the config file

- What's the per-stream windowMaximized for?

-----
- Do the controls scale poorly at other resolutions? The bottom of the Settings window covers the
    controls, but when? Is it DPI-aware? Need to ask.
- The stream overlaps the bottom border of the main window slightly, but only at very low
    resolutions

- Even after a Hungarian installation, it apparently starts up in English on a Hungarian Windows
  system — why?

- Sometimes the username/password is included in the RTSP URL, sometimes it isn't (only with the
  operational room camera, or is there another reason?)

-----
- The frames per second dropped to 8-10 instead of 25?? - Only after a while; could it be a virtual
  machine issue? Or the ffpmeg parameters?

*****
